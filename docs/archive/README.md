# Documentation Archive

This directory contains completed implementation plans retained as historical
records. Commands, paths, line numbers, branch names, and unchecked boxes may
be stale.

For current work, use the [documentation index](../README.md), source code,
and tests. Update an active design record instead of treating an archived plan
as a new task list.
